simplest-solr-config
====================

The smallest Solr config possible as a counter-balance to the example that ships with Solr itself.

Relies on Solr 4.9 to not need *fields* and *types* sections anymore.
